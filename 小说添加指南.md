# 小说添加指南

本文档提供了在系统中添加新小说的详细操作指南。请注意，本系统已摒弃自动识别功能，采用代码写死方式添加小说内容。

## 添加小说步骤

### 1. 准备小说信息

在添加小说前，请准备好以下信息：
- 小说标题
- 小说作者
- 小说分类（可选值：悬疑、都市、玄幻、仙侠、历史、科幻、言情、武侠）
- 小说内容（按段落划分）

### 2. 修改数据文件

系统中的小说数据存储在 `app.js` 文件中，具体是 `novels` 数组。请按照以下步骤修改：

#### 2.1 打开 app.js 文件

使用任何文本编辑器（如 VSCode、记事本等）打开 `app.js` 文件。

#### 2.2 定位 novels 数组

在文件中查找 `novels` 数组定义，通常在文件的开头部分。

#### 2.3 添加新小说对象

在 `novels` 数组中添加新的小说对象，格式如下：

```javascript
// 示例：添加新小说
let novels = [
    // 现有小说...
    {
        id: "novel_3", // 新的唯一ID，建议使用 novel_ 前缀 + 数字
        title: "新小说标题", // 小说标题
        author: "作者名称", // 作者名称
        category: "都市", // 小说分类
        content: [
            "这是小说的第一段内容。",
            "这是小说的第二段内容。",
            "这是小说的第三段内容。",
            // 可以添加更多段落...
        ]
    }
];
```

### 3. 确保ID唯一

请确保新添加的小说 ID 是唯一的，不要与已有的小说 ID 重复。建议使用 `novel_` 前缀加上数字的形式，并且数字应大于现有的最大数字。

### 4. 保存文件

添加完成后，保存 `app.js` 文件。

### 5. 测试新添加的小说

刷新网页，在小说列表中应该能看到新添加的小说。点击小说卡片，应该能正确显示小说内容。

## 完整代码示例

以下是一个完整的添加新小说的代码示例：

```javascript
// 假设现有小说数组
let novels = [
    {
        id: "novel_1",
        title: "渡口",
        author: "未知",
        category: "悬疑",
        content: [
            "我站在渡口边，看着江面上的雾气逐渐散去。",
            "远处传来船桨划水的声音，一艘小船缓缓驶来。"
        ]
    },
    {
        id: "novel_2",
        title: "城市之光",
        author: "李明",
        category: "都市",
        content: [
            "夜晚的城市灯火辉煌，李明站在高楼的窗前。",
            "他望着远处的霓虹灯，陷入了沉思。"
        ]
    },
    // 以下是新添加的小说
    {
        id: "novel_3",
        title: "星际迷航",
        author: "张华",
        category: "科幻",
        content: [
            "宇宙飞船在浩瀚的星海中航行，目的地是遥远的半人马座α星。",
            "船员们已经在太空中度过了漫长的三年时光。",
            "突然，警报声响起，雷达显示前方有不明物体接近。"
        ]
    }
];
```

## 注意事项

1. 小说内容应该按自然段落划分，每个段落作为数组的一个元素。
2. 确保分类名称与系统中预定义的分类一致，否则分类搜索功能可能无法正确识别。
3. 如果小说内容较长，建议分批添加段落，避免单个文件过大影响加载速度。
4. 保存文件时，请确保使用 UTF-8 编码，以正确显示中文。

## 常见问题

### Q: 添加小说后，列表中没有显示新小说怎么办？

A: 请检查以下几点：
- 确保小说对象的格式正确，所有必需的属性（id、title、author、category、content）都已包含
- 检查 id 是否唯一，没有与现有小说重复
- 尝试刷新网页，或者清除浏览器缓存后再刷新

### Q: 分类搜索功能无法找到新添加的小说怎么办？

A: 请确保小说的 category 属性值与系统中预定义的分类完全一致，包括大小写和中文字符。

### Q: 小说内容显示不完整或格式不正确怎么办？

A: 请检查 content 数组的格式，确保每个段落都是正确的字符串元素，并且数组语法正确。